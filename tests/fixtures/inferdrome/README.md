# Inferdrome importer fixtures

These are sealed, synthetic test bundles generated by Inferdrome's fake and
pinned-vLLM integration fixtures. They exercise interoperability and adversarial
ingestion only. They are not customer evidence. Tests copy them to a temporary
directory, bind the vLLM copy to a synthetic frozen ExitSpec contract when
needed, and recompute its exact-byte manifest.
